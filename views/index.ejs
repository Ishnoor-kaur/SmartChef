<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SmartChef - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-orange-50 min-h-screen flex flex-col items-center justify-center font-sans">
  <div class="text-center mb-8">
    <h1 class="text-4xl font-bold text-orange-600">🍳 SmartChef</h1>
    <p class="text-gray-700 mt-2">Enter ingredients you have. Get recipes instantly!</p>
  </div>

  <form action="/get_recipes" method="POST" class="w-full max-w-md bg-white p-6 rounded-2xl shadow-lg">
    <label for="ingredients" class="block text-lg font-semibold text-gray-800 mb-2">Ingredients (comma-separated)</label>
    <input
      type="text"
      id="ingredients"
      name="ingredients"
      required
      class="w-full border border-gray-300 rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-orange-400 mb-4"
      placeholder="e.g. tomato, onion, cheese"
    />
    <label for="diet" class="block text-lg font-semibold text-gray-800 mb-2 mt-4">Diet Preference</label>
    <select
    id="diet"
    name="diet"
    class="w-full border border-gray-300 rounded-xl p-3 focus:outline-none focus:ring-2 focus:ring-orange-400 mb-4"
    required
    >
    <option value="mixed">Mixed</option>
    <option value="vegetarian">Vegetarian</option>
    <option value="non-vegetarian">Non-Vegetarian</option>
    </select>

    <button
      type="submit"
      class="w-full bg-orange-500 hover:bg-orange-600 text-white font-semibold py-2 px-4 rounded-xl transition duration-200"
    >
      Get Recipes
    </button>
  </form>
</body>
</html>
