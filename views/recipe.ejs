<!-- <!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com">

  </script>
</head>
    <link rel="stylesheet" href="/styles.css">
<body class="bg-orange-50 p-6">
  <div class="max-w-4xl mx-auto bg-white rounded-2xl shadow-lg p-6">
    <h1 class="text-3xl font-bold text-orange-600 mb-4"><%= title %></h1>
    <img src="<%= image %>" alt="Recipe Image" class="rounded-xl w-full h-64 object-cover mb-4" />
    <p class="text-gray-700 mb-6"><%= description %></p>

    <h2 class="text-xl font-semibold mb-2">Ingredients</h2>
    <ul class="list-disc list-inside mb-6">
      <% ingredients.forEach(item => { %>
        <li><%= item %></li>
      <% }) %>
    </ul>
    <div class="recipe-container">
  <h1><%= recipe.title %></h1>

  <h2>Ingredients</h2>
  <ul>
    <% recipe.ingredients.forEach(function(ingredient) { %>
      <li><%= ingredient %></li>
    <% }); %>
  </ul>

  <h2>Instructions</h2>
  <ol>
    <% recipe.instructions.forEach(function(instruction) { %>
      <li><%= instruction %></li>
    <% }); %>
  </ol>
</div>

    <h2 class="text-xl font-semibold mb-2">Instructions</h2>
    <ol class="list-decimal list-inside space-y-2">
      <% instructions.forEach(step => { %>
        <li><%= step %></li>
      <% }) %>
    </ol>

    <div class="mt-6 text-center">
      <a href="/" class="text-orange-600 hover:underline">🔙 Back to Search</a>
    </div>
  </div>
</body>
</html> -->

<!DOCTYPE html>
<html>
<head>
  <title><%= recipe.title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1><%= recipe.title %></h1>
  <img src="<%= recipe.image %>" alt="Recipe Image">
  <p><%= recipe.description %></p>

  <h2>Ingredients</h2>
  <ul>
    <% recipe.ingredients.forEach(function(ingredient) { %>
      <li><%= ingredient %></li>
    <% }); %>
  </ul>

  <h2>Instructions</h2>
  <ol>
    <% recipe.instructions.forEach(function(instruction) { %>
      <li><%= instruction %></li>
    <% }); %>
  </ol>
</body>
</html>

